EXAMPLE
=============
This file demonstrates how to run the PMAnalyzer pipeline on given data.

First, open a Unix, OSX, or Linux terminal and go to the sample/ directory.

> cd ~/PMAnalyzer/sample

Here, the data_csedlakii/ directory is located. To run PMAnalyzer, run the command

> ../runPM -i data_csedlakii -n results -o myoutput

To provide the MAP information, run the command

> ../runPM -i data_csedlakii -n results -o myoutput -p sample_plate.txt

To generate figures and the growth heat-map

> ../runPM -i data_csedlakii -n results -o myoutput -p sample_plate.txt -m

Note: use the -v flag to produce a verbose output.

Sample ouput and images can be found in the `sample_results/` directory.
